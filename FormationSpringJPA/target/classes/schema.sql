DROP TABLE IF EXISTS t_user;
CREATE TABLE t_user (
id INT NOT NULL AUTO_INCREMENT
, login VARCHAR(50) NOT NULL
, pwd VARCHAR(50) NOT NULL
, firstname VARCHAR(50) NOT NULL
, lastname VARCHAR(50) NOT NULL
, UNIQUE UQ_USER_1 (login, pwd)
, PRIMARY KEY (ID)
);
